<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
        <li class="nav-a">{{#link-to "users"}}Users{{/link-to}}</li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li class="nav-a">{{#link-to "user_instruments"}}My Instruments{{/link-to}}</li>
        <li class="nav-a">{{#link-to "instruments"}}Instruments{{/link-to}}</li>
        <li class="nav-a">{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li class="nav-a"><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li class="nav-a">{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li class="nav-a">{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

{{outlet}}
